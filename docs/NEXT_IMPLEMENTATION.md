# Next implementation target

The next implementation pass should build on the locked decisions 16–31:
1. Browse and Album Detail UI.
2. Central selection controller and Select All flow.
3. Central file-operation service with conflict prompt and immediate Undo.
4. Favorites as a virtual collection.
5. Search across filenames, folders, albums, and metadata.
6. Settings backed by persistent preferences.

Security-sensitive Private implementation remains a dedicated later phase; no cosmetic file-extension trick is considered sufficient.
